# Reproducibility archive environment (pinned)
# Evidence source for each version: see software_versions.txt
numpy==2.2.5
pandas==2.3.2
scipy==1.16.2
scikit-learn==1.7.2
statsmodels==0.14.5
matplotlib==3.10.3
openpyxl==3.1.5
# Optional — only needed for the algorithm-benchmark script (it degrades
# gracefully via try/except if these are absent):
lightgbm==4.6.0
xgboost==3.4.1
catboost==1.2.10
